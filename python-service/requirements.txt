fastapi==0.116.1
huggingface_hub[hf_xet]<1.0
librosa>=0.10.0
numpy==2.3.4
requests==2.32.3
# Install resemblyzer with --no-deps (webrtcvad needs a C compiler on Windows)
# then install librosa separately. Both are already done in the project venv.
resemblyzer>=0.1.1
soundfile>=0.12.0
speechbrain==1.0.3
torch==2.8.0
torchaudio==2.8.0
typing>=3.7.4
uvicorn[standard]==0.35.0
webrtcvad-wheels>=2.0.10
